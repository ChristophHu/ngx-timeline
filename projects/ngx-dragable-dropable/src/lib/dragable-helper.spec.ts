import { DragableHelper } from './dragable-helper';

describe('DragableHelperDirective', () => {
  it('should create an instance', () => {
    const directive = new DragableHelper();
    expect(directive).toBeTruthy();
  });
});
